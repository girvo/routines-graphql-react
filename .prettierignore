# Generated by `pnpm --filter @my-routines/backend resolvers:generate`.
# graphql-codegen emits unformatted output and has no prettier step (its
# output-level lifecycle hooks are broken upstream — see the note in
# backend/codegen.ts), so formatting these files here would be undone by the
# next codegen run.
backend/src/graphql/resolver-types.ts
backend/tests/gql/

# Relay-compiler output: same reasoning as the backend codegen exclusion
# above — regenerated by `pnpm --filter @my-routines/frontend relay`, and a
# prettier pass would be undone by the next run.
**/__generated__/

# Lockfile
pnpm-lock.yaml

# Build output
frontend/dist/
